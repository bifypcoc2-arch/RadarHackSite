import Stripe from "stripe";import {NextResponse} from "next/server";export async function POST(req:Request){const fd=await req.formData(),plan=fd.get("plan")==="lifetime"?"lifetime":"monthly",price=plan==="lifetime"?process.env.STRIPE_LIFETIME_PRICE_ID:process.env.STRIPE_MONTHLY_PRICE_ID;if(!process.env.STRIPE_SECRET_KEY||!price)return NextResponse.redirect(new URL("/pricing?checkout=configure-stripe",req.url),303);const stripe=new Stripe(process.env.STRIPE_SECRET_KEY);const session=await stripe.checkout.sessions.create({mode:"payment",line_items:[{price,quantity:1}],success_url:`${process.env.NEXT_PUBLIC_APP_URL}/dashboard?payment=success`,cancel_url:`${process.env.NEXT_PUBLIC_APP_URL}/pricing?payment=cancelled`,metadata:{plan}});return NextResponse.redirect(session.url!,303)}
