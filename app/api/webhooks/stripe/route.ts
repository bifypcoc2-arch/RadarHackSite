import Stripe from "stripe";import {headers} from "next/headers";export async function POST(req:Request){if(!process.env.STRIPE_SECRET_KEY||!process.env.STRIPE_WEBHOOK_SECRET)return new Response("Stripe not configured",{status:503});const stripe=new Stripe(process.env.STRIPE_SECRET_KEY),body=await req.text(),sig=(await headers()).get("stripe-signature");if(!sig)return new Response("Missing signature",{status:400});try{const event=stripe.webhooks.constructEvent(body,sig,process.env.STRIPE_WEBHOOK_SECRET);if(event.type==="checkout.session.completed")console.info("Checkout completed",event.data.object.id);return Response.json({received:true})}catch(e){return new Response(`Webhook error: ${e instanceof Error?e.message:"Unknown"}`,{status:400})}}
